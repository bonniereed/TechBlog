<div class="text-center">
  {{}}
  <h2>{{post.id}}</h2>
  <p>{{post.title}}</p>
  <p>Created by {{post.user_id}} on {{post.date_created}}</p>
  <p>
    <span for="img" aria-label="money"></span>
    <span class="dollar-amount">{{text}}</span>
  </p>
  <p>{{post.date_created}</p>
  <p>
    <button class="btn btn-primary">Feel free to comment!</button>
  </p>
</div>